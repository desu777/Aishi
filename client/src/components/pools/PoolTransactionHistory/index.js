import PoolTransactionHistoryLogic from './PoolTransactionHistory.Logic';

const PoolTransactionHistory = ({ pool, theme, darkMode }) => {
  return <PoolTransactionHistoryLogic pool={pool} theme={theme} darkMode={darkMode} />;
};

export default PoolTransactionHistory; 